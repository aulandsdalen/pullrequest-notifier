%nav.navbar.navbar-static-top.navbar-inverse
	.container
		.navbar-header
			%span.navbar-brand= 'JC ' + locals[:version]
		%ul.nav.navbar-nav
			%li{:class => ("active" if locals[:active_requests])}
				%a{:href => "/requests"} Requests
			%li{:class => ("active" if locals[:active_users])}
				%a{:href => "/students"} Students
			%li{:class => ("active" if locals[:active_tasks])}
				%a{:href => "/tasks"} Tasks
			%li{:class => ("active" if locals[:active_info])}
				%a{:href => "/info"} Info
		.navbar-collapse.collapse
			%ul.nav.navbar-nav.navbar-right
				%li
					%a{:href => "/logout"} Log out
